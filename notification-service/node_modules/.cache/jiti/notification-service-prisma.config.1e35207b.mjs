"use strict"; // Forward to the JS config so Prisma CLI doesn't need TypeScript tooling.
// This keeps the file present but ensures the CLI can parse it.
module.exports = require('./prisma.config.js'); /* v9-bc70131ec7f0872a */
